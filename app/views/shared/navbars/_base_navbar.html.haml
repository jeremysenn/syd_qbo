-#
  .navbar.navbar-default{ role: 'navigation', style: 'margin-bottom: 0px;'}
.navbar.navbar-default.navbar-fixed-top{ role: 'navigation'}
  .container-fluid
    .navbar-header
      %button.navbar-toggle{ type: 'button', data: { toggle: 'collapse', target: '.navbar-collapse' } }
        %span.sr-only= t('.toggle_navigation')
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      -#
        %a{class: 'navbar-brand', :href => root_path, :style => "color: #2563AB"}
      %a{class: 'navbar-brand', :href => root_path}
        SYD QBO
    .collapse.navbar-collapse
      -#
        %ul.nav.navbar-nav
          %li{:class => params[:controller] == "images" ? "active" : ""}
            %a{href: images_path}
              Tickets
      %ul.nav.navbar-nav.navbar-right
        %li= link_to "<strong>#{current_user.email}</strong>".html_safe, edit_user_registration_path(current_user)
        %li= link_to 'Log out', destroy_user_session_path, method: :delete, data: { confirm: 'Are you sure you want to log out?' }